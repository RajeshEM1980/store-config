spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    stream:
      bindings:
        input_file_created:
          destination: itemsavedfilelocation
        input_file_deleted:
          destination: itemdeletedfilelocation
